{
  "private": true,
  "name": "arbitrum-dai-bridge",
  "version": "0.1.0",
  "workspaces": [
    "packages/bridge",
    "packages/arbitrum/packages/arb-bridge-eth",
    "packages/arbitrum/packages/arb-bridge-peripherals",
    "packages/arbitrum/packages/arb-os/contracts"
  ],
  "scripts": {
    "build": "yarn workspace bridge build",
    "typecheck": "yarn workspace bridge typecheck",
    "lint": "yarn workspace bridge lint",
    "format": "yarn workspace bridge format",
    "test": "yarn workspace bridge test",
    "test-e2e": "yarn workspace bridge test-e2e",
    "test:fix": "yarn workspace bridge test:fix",
    "certora": "certoraRun --solc ~/.solc-select/artifacts/solc-0.6.11 contracts/l2/dai.sol:Dai certora/HashHelper.sol --verify Dai:certora/dai.spec --rule_sanity --solc_args \"['--optimize','--optimize-runs','200']\""
  }
}
